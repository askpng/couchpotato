[Unit]
Description=flex-launcher service
After=graphical-session.target

[Service]
ExecStart=/usr/bin/weston -c /etc/xdg/weston/weston-flex.ini
# Restart=on-failure
# RestartSec=1

[Install]
# Hook into the default user session target
WantedBy=default.target
